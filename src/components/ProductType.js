import React from 'react';

const ProductType = ({ productType }) => {
    return (
        <div className='prodyuct__type'>
            {productType}
        </div>
    );
}

export default ProductType;
